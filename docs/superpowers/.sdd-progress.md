# subBeta8 Keyboard Editing — Progress Ledger

Plan: docs/superpowers/plans/2026-06-19-subbeta8-keyboard-boundary-editing.md
Working file: /web/zin/subBeta8.html
No git in /web/zin → per-task backups (subBeta8.html.backup_taskN) stand in for commits; reviewer diffs backup_{N-1} vs current.

- Task 1: complete (clone subBeta4→subBeta8, baseline backup created)
- Task 2: complete (selection state, click-select, highlight; spec ✅, quality approved, only benign Minors; guard classes verified)
- Task 3: complete (FRAME/snapToFrame, 3 nudge fns w/ PUSH HOOK comments, arrow-key branch; spec ✅, quality approved, Minors only)
- Task 4: complete (pushNeighbors + room guards wired into nudges; spec ✅, quality approved). MINOR for final review: room guards are conservative — sum ALL same-tier neighbors' lengths, so an extend can be refused even when gaps mean room exists. Acceptable per spec; revisit if annoying in browser test.
- Task 5: complete (Tab/Shift+Tab same-tier nav, nearest-on-empty; spec ✅, quality approved, Minors only)
- Task 6: complete (keyboard-controls button + Bootstrap modal; markup spec ✅, quality approved). Reviewer's "out-of-scope JS" FAIL adjudicated FALSE POSITIVE by controller: backup_task5 was stale (captured task4 state — Task 5 agent's cp ran at wrong moment), so the task5→task6 diff wrongly included Task 5's already-approved Tab JS. Live file verified: each logic block appears exactly once, node --check OK.
- FINAL REVIEW: READY TO MERGE. One Important (twin-box DOM desync under optional syncEnabled) → FIXED (updateTwinBox after every syncTwin in nudges+push; backup_fix1; node --check OK).
- Open MINORS for awareness (not bugs): (a) room guards conservative — may refuse an extend near video-end when gaps exist; (b) Tab "nearest" on empty selection isn't tier-filtered (picks nearest across all tiers, then walks that tier).
- Task 7: pending — browser regression (requires human). Test URL: https://signcollect.nl/zin/subBeta8.html?filename=M20260126_1590.wav
- Task 9: complete — side canvases (start/end frame previews) persist while a gloss is selected: shown in selectSub, updated in all 3 nudges, onEndThreeCanvases returns to selected gloss instead of hiding, cleared on delete. Respects Show-Side-Videos toggle; no drag/resize regression. Spec ✅, quality approved; node --check OK. Backup: backup_task9.
- Task 8: complete — select-vs-edit split (contentEditable=false default; click/Tab select, dblclick/Enter edit, Esc/Enter exit; glossary dropdown + Tab-hop preserved; modal updated). Spec ✅, quality approved; reviewer formally verified all 4 Esc/Enter×dropdown cases + tier0/1; node --check OK. Backup: backup_task8.
- NOTE: backup_task5 is stale (==task4 state). Does not affect final review (diffs baseline→current). All Task 5 work confirmed present in live file.
